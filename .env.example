# 复制为 .env 后，务必替换下面两个密码。
MYSQL_USER=surveyking
MYSQL_PASSWORD=change_this_database_password
MYSQL_ROOT_PASSWORD=change_this_root_password

# 宿主机对外端口
SURVEYKING_PORT=1991

# 按服务器内存调整
JAVA_OPTS=-Xms256m -Xmx1024m -Duser.timezone=Asia/Shanghai

# 不使用 AI 功能时保持默认值即可
AI_SILICONFLOW_TOKEN=your_token
